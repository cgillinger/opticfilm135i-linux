diff --git a/backend/Makefile.am b/backend/Makefile.am
index 64c6cb1..40ecbbc 100644
--- a/backend/Makefile.am
+++ b/backend/Makefile.am
@@ -765,6 +765,10 @@ libgenesys_la_SOURCES = genesys/genesys.cpp genesys/genesys.h \
     genesys/fwd.h \
     genesys/gl646.cpp genesys/gl646.h genesys/gl646_registers.h \
     genesys/gl124.cpp genesys/gl124.h genesys/gl124_registers.h \
+    genesys/gl126.cpp genesys/gl126.h genesys/gl126_registers.h \
+    genesys/gl126_tables.cpp genesys/gl126_tables.h \
+    genesys/gl126_ops.cpp genesys/gl126_ops.h \
+    genesys/gl126_lock.cpp genesys/gl126_lock.h \
     genesys/gl841.cpp genesys/gl841.h genesys/gl841_registers.h \
     genesys/gl842.cpp genesys/gl842.h genesys/gl842_registers.h \
     genesys/gl843.cpp genesys/gl843.h genesys/gl843_registers.h \
diff --git a/backend/genesys.conf.in b/backend/genesys.conf.in
index 6e6a8d7..1153059 100644
--- a/backend/genesys.conf.in
+++ b/backend/genesys.conf.in
@@ -169,3 +169,6 @@ usb 0x07b3 0x1300
 
 # Plustek OpticFilm 7600i
 usb 0x07b3 0x0c3b
+
+# Plustek OpticFilm 135i
+usb 0x07b3 0x1436
diff --git a/backend/genesys/enums.cpp b/backend/genesys/enums.cpp
index 343a220..34f64c6 100644
--- a/backend/genesys/enums.cpp
+++ b/backend/genesys/enums.cpp
@@ -118,6 +118,7 @@ std::ostream& operator<<(std::ostream& out, ModelId id)
         case ModelId::PANASONIC_KV_SS080: out << "PANASONIC_KV_SS080"; break;
         case ModelId::PENTAX_DSMOBILE_600: out << "PENTAX_DSMOBILE_600"; break;
         case ModelId::PLUSTEK_OPTICBOOK_3800: out << "PLUSTEK_OPTICBOOK_3800"; break;
+        case ModelId::PLUSTEK_OPTICFILM_135I: out << "PLUSTEK_OPTICFILM_135I"; break;
         case ModelId::PLUSTEK_OPTICFILM_7200: out << "PLUSTEK_OPTICFILM_7200"; break;
         case ModelId::PLUSTEK_OPTICFILM_7200I: out << "PLUSTEK_OPTICFILM_7200I"; break;
         case ModelId::PLUSTEK_OPTICFILM_7300: out << "PLUSTEK_OPTICFILM_7300"; break;
@@ -170,6 +171,7 @@ std::ostream& operator<<(std::ostream& out, SensorId id)
         case SensorId::CCD_PLUSTEK_OPTICBOOK_3800: out << "CCD_PLUSTEK_OPTICBOOK_3800"; break;
         case SensorId::CCD_PLUSTEK_OPTICFILM_7200: out << "CCD_PLUSTEK_OPTICFILM_7200"; break;
         case SensorId::CCD_PLUSTEK_OPTICFILM_7200I: out << "CCD_PLUSTEK_OPTICFILM_7200I"; break;
+        case SensorId::CCD_PLUSTEK_OPTICFILM_135I: out << "CCD_PLUSTEK_OPTICFILM_135I"; break;
         case SensorId::CCD_PLUSTEK_OPTICFILM_7300: out << "CCD_PLUSTEK_OPTICFILM_7300"; break;
         case SensorId::CCD_PLUSTEK_OPTICFILM_7400_V1: out << "CCD_PLUSTEK_OPTICFILM_7400_V1"; break;
         case SensorId::CCD_PLUSTEK_OPTICFILM_7400_V2: out << "CCD_PLUSTEK_OPTICFILM_7400_V2"; break;
diff --git a/backend/genesys/enums.h b/backend/genesys/enums.h
index 02cfc43..8813651 100644
--- a/backend/genesys/enums.h
+++ b/backend/genesys/enums.h
@@ -179,6 +179,7 @@ enum class ModelId : unsigned
     PANASONIC_KV_SS080,
     PENTAX_DSMOBILE_600,
     PLUSTEK_OPTICBOOK_3800,
+    PLUSTEK_OPTICFILM_135I,
     PLUSTEK_OPTICFILM_7200,
     PLUSTEK_OPTICFILM_7200I,
     PLUSTEK_OPTICFILM_7300,
@@ -241,6 +242,7 @@ enum class SensorId : unsigned
     CCD_PLUSTEK_OPTICBOOK_3800,
     CCD_PLUSTEK_OPTICFILM_7200,
     CCD_PLUSTEK_OPTICFILM_7200I,
+    CCD_PLUSTEK_OPTICFILM_135I,
     CCD_PLUSTEK_OPTICFILM_7300,
     CCD_PLUSTEK_OPTICFILM_7400_V1,
     CCD_PLUSTEK_OPTICFILM_7400_V2,
@@ -458,6 +460,7 @@ enum class AsicType : unsigned
     GL846,
     GL847,
     GL124,
+    GL126,   // Plustek OpticFilm 135i
 };
 
 
diff --git a/backend/genesys/genesys.cpp b/backend/genesys/genesys.cpp
index d7ec422..913ca4c 100644
--- a/backend/genesys/genesys.cpp
+++ b/backend/genesys/genesys.cpp
@@ -38,6 +38,11 @@
 #define DEBUG_NOT_STATIC
 
 #include "genesys.h"
+// GL126 (OpticFilm 135i) process lock: mutual exclusion with the of135i
+// driver, taken/released around sane_open/sane_close below. The rest of
+// the GL126 command set is reached through low.cpp's create_cmd_set(),
+// which is the only other file that needs gl126.h.
+#include "gl126_lock.h"
 #include "gl124_registers.h"
 #include "gl841_registers.h"
 #include "gl842_registers.h"
@@ -3657,11 +3662,19 @@ static void genesys_flatbed_calibration(Genesys_Device* dev, Genesys_Sensor& sen
     }
 
     // send default shading data
-    dev->interface->record_progress_message("sanei_genesys_init_shading_data");
-    sanei_genesys_init_shading_data(dev, sensor, pixels_per_line);
-
-    if (dev->settings.scan_method == ScanMethod::TRANSPARENCY ||
-        dev->settings.scan_method == ScanMethod::TRANSPARENCY_INFRARED)
+    // Not for GL126 (OpticFilm 135i): the default table would go to scanner
+    // RAM through write_buffer, a transfer this unit has never been driven
+    // with; its shading table is measured and uploaded by the vendor's own
+    // phases (docs/sane-hook3-gain.md, section 5).
+    if (dev->model->asic_type != AsicType::GL126) {
+        dev->interface->record_progress_message("sanei_genesys_init_shading_data");
+        sanei_genesys_init_shading_data(dev, sensor, pixels_per_line);
+    }
+
+    // Not for GL126 either: no calibration area to move to (see genesys_start_scan).
+    if ((dev->settings.scan_method == ScanMethod::TRANSPARENCY ||
+         dev->settings.scan_method == ScanMethod::TRANSPARENCY_INFRARED) &&
+        dev->model->asic_type != AsicType::GL126)
     {
         scanner_move_to_ta(*dev);
     }
@@ -3990,15 +4003,20 @@ static void genesys_start_scan(Genesys_Device* dev, bool lamp_off)
     }
 
   /* set top left x and y values by scanning the internals if flatbed scanners */
-    if (!dev->model->is_sheetfed) {
+    // Not for GL126 (OpticFilm 135i): the vendor never homes before a scan
+    // and has no calibration area to move to -- frame positioning is one
+    // absolute feed inside the scan sequence, and calibration happens at
+    // the current position (docs/sane-hook2-offset.md, section 4).
+    if (!dev->model->is_sheetfed && dev->model->asic_type != AsicType::GL126) {
         // TODO: check we can drop this since we cannot have the scanner's head wandering here
         dev->parking = false;
         dev->cmd_set->move_back_home(dev, true);
     }
 
   /* move to calibration area for transparency adapter */
-    if (dev->settings.scan_method == ScanMethod::TRANSPARENCY ||
-        dev->settings.scan_method == ScanMethod::TRANSPARENCY_INFRARED)
+    if ((dev->settings.scan_method == ScanMethod::TRANSPARENCY ||
+         dev->settings.scan_method == ScanMethod::TRANSPARENCY_INFRARED) &&
+        dev->model->asic_type != AsicType::GL126)
     {
         scanner_move_to_ta(*dev);
     }
@@ -5457,6 +5475,32 @@ static void sane_open_impl(SANE_String_Const devicename, SANE_Handle * handle)
         throw SaneException("could not find the device to open: %s", devicename);
     }
 
+    // GL126 (OpticFilm 135i) process lock: RAII guard covering the rest of
+    // this function. sane_open_impl acquires the lock and sane_close_impl
+    // releases it, but they are two different functions with an unbounded
+    // amount of throwing code in between (USB open, cmd_set->init,
+    // update_hardware_sensors) -- a try/catch around just the USB-open
+    // block missed all of that, and a catch(...) that released
+    // unconditionally would drop another GL126 session's lock if this
+    // open failed after that session had already added a reference
+    // (external review, 2026-09-08; see docs/sane-port.md, "Mutual
+    // exclusion with the driver"). The guard is armed right after a
+    // successful acquire and disarmed only once this function is about to
+    // return successfully -- from then, the new handle owns that
+    // reference and sane_close_impl releases it. Any throw in between
+    // releases exactly the one reference this open took; a non-GL126 open
+    // (or the testing-mode branch) never arms it, so it never touches the
+    // lock at all.
+    struct Gl126LockGuard {
+        bool armed = false;
+        ~Gl126LockGuard()
+        {
+            if (armed) {
+                gl126::process_lock_release();
+            }
+        }
+    } gl126_lock_guard;
+
     if (is_testing_mode()) {
         // during testing we need to initialize dev->model before test scanner interface is created
         // as that it needs to know what type of chip it needs to mimic.
@@ -5473,6 +5517,22 @@ static void sane_open_impl(SANE_String_Const devicename, SANE_Handle * handle)
 
         dev->interface->get_usb_device().open(dev->file_name.c_str());
     } else {
+        // GL126 (OpticFilm 135i): take the driver's process lock before any
+        // USB transfer (docs/sane-port.md, "Mutual exclusion with the
+        // driver"). Claiming the USB interface alone does not exclude the
+        // driver's read-only sessions (status, doctor), which hold this
+        // same lock without claiming the interface.
+        std::string gl126_lock_holder;
+        if (dev->model->asic_type == AsicType::GL126) {
+            if (!gl126::process_lock_acquire(&gl126_lock_holder)) {
+                throw SaneException(SANE_STATUS_DEVICE_BUSY,
+                                    "OpticFilm 135i is held by another process (%s); "
+                                    "nothing was sent to the scanner",
+                                    gl126_lock_holder.empty() ? "unknown" : gl126_lock_holder.c_str());
+            }
+            gl126_lock_guard.armed = true;
+        }
+
         dev->interface = std::unique_ptr<ScannerInterfaceUsb>{new ScannerInterfaceUsb{dev}};
 
         dbg.vstatus("open device '%s'", dev->file_name.c_str());
@@ -5523,6 +5583,12 @@ static void sane_open_impl(SANE_String_Const devicename, SANE_Handle * handle)
 
     // some hardware capabilities are detected through sensors
     dev->cmd_set->update_hardware_sensors (s);
+
+    // Reaching here means sane_open_impl is returning successfully: the
+    // new handle now owns the GL126 lock reference (if any was taken),
+    // and sane_close_impl releases it. Disarm so the guard's destructor
+    // -- run on this normal return, same as on a throw -- does nothing.
+    gl126_lock_guard.armed = false;
 }
 
 SANE_GENESYS_API_LINKAGE
@@ -5556,6 +5622,20 @@ sane_close_impl(SANE_Handle handle)
 
     auto* dev = it->dev;
 
+    // GL126: release the driver's process lock reference this handle took in
+    // sane_open_impl, at scope exit so that a throw anywhere below (eject,
+    // park wait, close) cannot leave it held inside a long-lived process.
+    // Runs after the USB close on the normal path.
+    struct Gl126LockRelease {
+        bool armed;
+        ~Gl126LockRelease()
+        {
+            if (armed) {
+                gl126::process_lock_release();
+            }
+        }
+    } gl126_lock_release{dev->model->asic_type == AsicType::GL126};
+
     // eject document for sheetfed scanners
     if (dev->model->is_sheetfed) {
         catch_all_exceptions(__func__, [&](){ dev->cmd_set->eject_document(dev); });
@@ -5579,12 +5659,26 @@ sane_close_impl(SANE_Handle handle)
     dev->clear();
 
     // LAMP OFF : same register across all the ASICs */
-    dev->interface->write_register(0x03, 0x00);
-
-    catch_all_exceptions(__func__, [&](){ dev->interface->get_usb_device().clear_halt(); });
-
-    // we need this to avoid these ASIC getting stuck in bulk writes
-    catch_all_exceptions(__func__, [&](){ dev->interface->get_usb_device().reset(); });
+    // Not for GL126: its reference driver writes nothing at session close,
+    // and the vendor's 0x03 = 0x00 only ever appears inside its PARK and
+    // cold-init sequences. A lone lamp-off on a loaded magazine left the
+    // unit in a state the following eject stalled from (Test 44).
+    if (dev->model->asic_type != AsicType::GL126) {
+        dev->interface->write_register(0x03, 0x00);
+    }
+
+    // The GL126 unit (OpticFilm 135i) has never been driven with a port
+    // reset or an endpoint clear-halt: its reference driver releases the
+    // interface and nothing else, and the unit is known to leave the bus
+    // for good after a session under some conditions. Whether a reset is
+    // safe is a separate bring-up check (docs/sane-port.md stage 3), so
+    // until it is made the close path does what the reference driver does.
+    if (dev->model->asic_type != AsicType::GL126) {
+        catch_all_exceptions(__func__, [&](){ dev->interface->get_usb_device().clear_halt(); });
+
+        // we need this to avoid these ASIC getting stuck in bulk writes
+        catch_all_exceptions(__func__, [&](){ dev->interface->get_usb_device().reset(); });
+    }
 
     // not freeing dev because it's in the dev list
     catch_all_exceptions(__func__, [&](){ dev->interface->get_usb_device().close(); });
diff --git a/backend/genesys/low.cpp b/backend/genesys/low.cpp
index 3ffc24a..eda72e8 100644
--- a/backend/genesys/low.cpp
+++ b/backend/genesys/low.cpp
@@ -35,6 +35,7 @@
 #include "gl646_registers.h"
 
 #include "gl124.h"
+#include "gl126.h"
 #include "gl646.h"
 #include "gl841.h"
 #include "gl842.h"
@@ -65,6 +66,7 @@ std::unique_ptr<CommandSet> create_cmd_set(AsicType asic_type)
         case AsicType::GL846: return std::unique_ptr<CommandSet>(new gl846::CommandSetGl846{});
         case AsicType::GL847: return std::unique_ptr<CommandSet>(new gl847::CommandSetGl847{});
         case AsicType::GL124: return std::unique_ptr<CommandSet>(new gl124::CommandSetGl124{});
+        case AsicType::GL126: return std::unique_ptr<CommandSet>(new gl126::CommandSetGl126{});
         default: throw SaneException(SANE_STATUS_INVAL, "unknown ASIC type");
     }
 }
@@ -139,7 +141,8 @@ Status scanner_read_status(Genesys_Device& dev)
     std::uint16_t address = 0;
 
     switch (dev.model->asic_type) {
-        case AsicType::GL124: address = 0x101; break;
+        case AsicType::GL124:
+        case AsicType::GL126: address = 0x101; break;
         case AsicType::GL646:
         case AsicType::GL841:
         case AsicType::GL842:
diff --git a/backend/genesys/scanner_interface_usb.cpp b/backend/genesys/scanner_interface_usb.cpp
index 542d4ff..dd77f3c 100644
--- a/backend/genesys/scanner_interface_usb.cpp
+++ b/backend/genesys/scanner_interface_usb.cpp
@@ -43,7 +43,8 @@ std::uint8_t ScannerInterfaceUsb::read_register(std::uint16_t address)
     if (dev_->model->asic_type == AsicType::GL847 ||
         dev_->model->asic_type == AsicType::GL845 ||
         dev_->model->asic_type == AsicType::GL846 ||
-        dev_->model->asic_type == AsicType::GL124)
+        dev_->model->asic_type == AsicType::GL124 ||
+        dev_->model->asic_type == AsicType::GL126)
     {
         std::uint8_t value2x8[2];
         std::uint16_t address16 = 0x22 + (address << 8);
@@ -88,7 +89,8 @@ void ScannerInterfaceUsb::write_register(std::uint16_t address, std::uint8_t val
     if (dev_->model->asic_type == AsicType::GL847 ||
         dev_->model->asic_type == AsicType::GL845 ||
         dev_->model->asic_type == AsicType::GL846 ||
-        dev_->model->asic_type == AsicType::GL124)
+        dev_->model->asic_type == AsicType::GL124 ||
+        dev_->model->asic_type == AsicType::GL126)
     {
         std::uint8_t buffer[2];
 
@@ -438,7 +440,9 @@ void ScannerInterfaceUsb::write_fe_register(std::uint8_t address, std::uint16_t
     Genesys_Register_Set reg(Genesys_Register_Set::SEQUENTIAL);
 
     reg.init_reg(0x51, address);
-    if (dev_->model->asic_type == AsicType::GL124) {
+    if (dev_->model->asic_type == AsicType::GL124 ||
+        dev_->model->asic_type == AsicType::GL126)
+    {
         reg.init_reg(0x5d, (value / 256) & 0xff);
         reg.init_reg(0x5e, value & 0xff);
     } else {
diff --git a/backend/genesys/tables_model.cpp b/backend/genesys/tables_model.cpp
index 321d179..47308a6 100644
--- a/backend/genesys/tables_model.cpp
+++ b/backend/genesys/tables_model.cpp
@@ -2311,6 +2311,91 @@ void genesys_init_usb_device_tables()
 
 
 
+    /* Plustek OpticFilm 135i (GL126).
+
+       STAGE 1 SKELETON, UNTESTED. The sensor/adc/gpio/motor ids below are
+       the 7200's, used as placeholders so the model registers and the
+       backend builds: the 135i's own tables are not written yet, and
+       every scan hook in gl126.cpp refuses before anything reaches the
+       wire, so the placeholder values cannot drive the hardware. They are
+       replaced with measured ones during stage 3 bring-up
+       (docs/sane-port.md).
+
+       Geometry is the 35 mm frame as the driver scans it: a 36 x 24 mm
+       image area on a strip fed through the magazine. */
+    model = Genesys_Model();
+    model.name = "plustek-opticfilm-135i";
+    model.vendor = "PLUSTEK";
+    model.model = "OpticFilm 135i";
+    model.model_id = ModelId::PLUSTEK_OPTICFILM_135I;
+    model.asic_type = AsicType::GL126;
+
+    model.resolutions = {
+        {
+            { ScanMethod::TRANSPARENCY },
+            { 7200, 3600, 2400, 1200, 600 },
+            { 7200, 3600, 2400, 1200, 600 },
+        }
+    };
+
+    model.bpp_gray_values = { 16 };
+    model.bpp_color_values = { 16 };
+    model.default_method = ScanMethod::TRANSPARENCY;
+
+    model.x_offset = 0.0;
+    model.y_offset = 0.0;
+    model.x_size = 36.0;
+    model.y_size = 24.0;
+
+    model.y_offset_calib_white = 0.0;
+    model.y_size_calib_mm = 0.0;
+    model.x_offset_calib_black = 0.0;
+    model.x_size_calib_mm = 36.0;
+
+    model.x_offset_ta = 0.0;
+    model.y_offset_ta = 0.0;
+    model.x_size_ta = 36.0;
+    model.y_size_ta = 24.0;
+
+    model.y_offset_sensor_to_ta = 0.0;
+    model.y_offset_calib_black_ta = 0.0;
+    model.y_offset_calib_white_ta = 0.0;
+    model.y_size_calib_ta_mm = 0.0;
+
+    model.post_scan = 0.0;
+    model.eject_feed = 0.0;
+
+    /* Colour-line stagger at 3600 dpi: 24 lines between R and B
+       (docs/protocol-notes.md pass 18). */
+    model.ld_shift_r = 0;
+    model.ld_shift_g = 12;
+    model.ld_shift_b = 24;
+
+    model.line_mode_color_order = ColorOrder::RGB;
+
+    model.is_cis = false;
+    /* Not sheetfed: that path ejects after every scan and reloads before
+       calibration, which breaks a 1-4 frame batch (docs/sane-port.md,
+       decision 4). */
+    model.is_sheetfed = false;
+
+    model.sensor_id = SensorId::CCD_PLUSTEK_OPTICFILM_135I;
+    /* Placeholders -- see the note above. */
+    model.adc_id = AdcId::PLUSTEK_OPTICFILM_7200;
+    model.gpio_id = GpioId::PLUSTEK_OPTICFILM_7200;
+    model.motor_id = MotorId::PLUSTEK_OPTICFILM_7200;
+
+    /* No ModelFlag::WARMUP: the core's warm-up moves the head to the
+       calibration area and runs a lamp warm-up before every scan, neither
+       of which the vendor does on this unit. The lamp warm-up that exists
+       is the retry inside the gain calibration (docs/sane-hook2-offset.md,
+       section 4). */
+    model.flags = ModelFlag::UNTESTED;
+
+    model.search_lines = 200;
+    s_usb_devices->emplace_back(0x07b3, 0x1436, model);
+
+
     model = Genesys_Model();
     model.name = "plustek-opticfilm-7200";
     model.vendor = "PLUSTEK";
diff --git a/backend/genesys/tables_sensor.cpp b/backend/genesys/tables_sensor.cpp
index 09ca861..d40a24e 100644
--- a/backend/genesys/tables_sensor.cpp
+++ b/backend/genesys/tables_sensor.cpp
@@ -3355,6 +3355,55 @@ void genesys_init_sensor_tables()
     }
 
 
+    /* Plustek OpticFilm 135i (GL126). The GL126 command set drives the
+       chip from register tables generated from USB captures
+       (gl126_tables.cpp), so the per-sensor register and calibration
+       fields the core uses for other chips are not consulted; what the
+       core does read from here is the resolution list, the scan method
+       and the channel count. The rest is left at its defaults and marked
+       as such rather than copied from another sensor. */
+    sensor = Genesys_Sensor();
+    sensor.sensor_id = SensorId::CCD_PLUSTEK_OPTICFILM_135I; // gl126
+    sensor.full_resolution = 7200;
+    sensor.optical_resolution = 7200;
+    sensor.register_dpihw = 3600; // not used by the GL126 hooks
+    sensor.black_pixels = 0;      // calibration is table-driven in gl126.cpp
+    sensor.dummy_pixel = 0;
+    sensor.fau_gain_white_ref = 0;
+    sensor.gain_white_ref = 0;
+    sensor.exposure = { 0x0000, 0x0000, 0x0000 };
+    /* Not consumed by the GL126 hooks: the captured tables carry the
+       per-channel exposure block (regs 0xe0-0xf7, DPI-independent). The
+       core still needs a non-negative value here to seed its exposure
+       option; 0x3ffb is the end of that captured block. */
+    sensor.exposure_lperiod = 0x3ffb;
+    sensor.gamma = { 1.0f, 1.0f, 1.0f };
+    {
+        struct CustomSensorSettings
+        {
+            ValueFilterAny<unsigned> resolutions;
+            unsigned register_dpiset;
+        };
+
+        CustomSensorSettings custom_settings[] = {
+            { { 600 }, 600 },
+            { { 1200 }, 1200 },
+            { { 2400 }, 2400 },
+            { { 3600 }, 3600 },
+            { { 7200 }, 7200 },
+        };
+
+        for (const CustomSensorSettings& setting : custom_settings) {
+            sensor.resolutions = setting.resolutions;
+            sensor.method = ScanMethod::TRANSPARENCY;
+            sensor.shading_resolution = setting.resolutions.values().front();
+            sensor.register_dpiset = setting.register_dpiset;
+            sensor.pixel_count_ratio = Ratio{1, 1};
+            sensor.output_pixel_offset = 0;
+            s_sensors->push_back(sensor);
+        }
+    }
+
     sensor = Genesys_Sensor();
     sensor.sensor_id = SensorId::CCD_PLUSTEK_OPTICFILM_7300; // gl843
     sensor.full_resolution = 7200;
diff --git a/doc/descriptions/genesys.desc b/doc/descriptions/genesys.desc
index 43b0736..83414f8 100644
--- a/doc/descriptions/genesys.desc
+++ b/doc/descriptions/genesys.desc
@@ -17,6 +17,12 @@
 :status :basic
 :comment "Has a Primax USB ID"
 
+:model "OpticFilm 135i"
+:interface "USB"
+:usbid "0x07b3" "0x1436"
+:status :untested
+:comment "GL126; backend skeleton, hardware bring-up in progress"
+
 :model "OpticFilm 7200"
 :interface "USB"
 :usbid "0x07b3" "0x0807"
